<!-- ── Cabeçalho da página ── -->
<div class="saved-header">
    <h1><i class="fa-solid fa-bookmark"></i> Guardados</h1>
    <span class="saved-count">
        <?= number_format($total) ?> item<?= $total !== 1 ? 's' : '' ?>
    </span>
</div>
